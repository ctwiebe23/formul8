# NAME

form_factor — Automatic form handling

# SYNOPSIS

```js
import { form_factor } from "form_factor"

const { form_data, change_event } = form_factor("my_form_id")

do_stuff(form_data)
document.addEventListener(change_event, () => do_stuff(form_data))
```

# DESCRIPTION

2DO

# EXAMPLE

2 FAST 2 DO
