260119	0.4.4 -- Changed order of event name choice
	When choosing the name of a form's corresponding change event, formul8
	now prioritizes the form's ID over the form's name.
251125  0.4.3 -- Bugfixes
	Fixed bug where nested fieldsets woudn't be caught by gather_inputs.
251124	0.4.2 -- Better typecasting
	Redid typecasting inside internal functions.
251122	0.4.1 -- Removed insistence that targets must be forms
	Now, any HTML element can be an input to formul8.
	Extended documentation.
251121  0.4.0 -- Recursively gather inputs
	Now gathers inputs that are nested within a form, not just at the form
	root.
251116	0.3.4 -- New defaults
	Default debounce is now 0.
	Default events now include the submit event.
251115  0.3.3 -- Bugfixes
	Replaced lone undefined with the cooler void 0.
	Safely dereferenced JSON obj using null-safe-chain-operator-thing.
	Cast number input value to number type.
251115	0.3.1 -- Better handling of fieldset legends as IDs
	Now replaces spaces with underscores when using a fieldset's legend's
	innerText as an ID.
251115	0.3.0 -- Added bidirectional updates
	Changing the state object now also changes the form itself, and can
	optionally auto-fire the change event.
	Added options to alter direction and auto-notify.
	Updated test.html to demonstrate new feature.
251113	0.2.0 -- Published to NPM
	Published the project to NPM under the name formul8 (previously
	known as form_factor).
	Added test.html as an example.
	Made it so that when a legend is taken as an identifier the name is
	first lowercased.
251113	0.1.3 -- Better handling of unidentifiable elements
	More descriptive console.warn message in fallback_id, and fallback_id
	now assigns the identifier it comes up with to the unidentifiable
	element it is identifying.
251112	0.1.2 -- More options
	Added an option to override the default events, and moved debounce
	into the option object as well.
	Also updated the logic to recognize textareas and selects as input-ish.
251112	0.1.1 -- README stuff
	Bare minimum README.
251112	0.1.0 -- Initial push
	Configured github source control and all that jazz.
251112	0.0.4 -- Documentation and formatting
	I finally gave in and wrote a single blessed comment.
	Also whitespace, I didn't have of that before this point.
251112	0.0.3 -- TypeScript
	Converted to TypeScript for the generated declaration file.
251112	0.0.2 -- Custom events and debouncing
	Added a custom event that fires when the value obj updates, and
	a debounce system to prevent that from happening all the time.
251111	0.0.1 -- Checkbox exceptions
	Now handles checkboxes differently than other inputs.
251111	0.0.0 -- Initial draft
	Start of project; some thrown-together recursive functions.
	Base functionality implemented.
